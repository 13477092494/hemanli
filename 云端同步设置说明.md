# 跨设备实时同步 —— 已修复并部署（Supabase）

## 问题根因
原来的数据只存在 `localStorage` + `BroadcastChannel`，只能在**同一台设备的同一个浏览器**内同步。客户用手机扫码提交的预约，写进的是手机浏览器的存储；你的工作台开在另一台设备，两边互不相通，所以「收不到消息」。

## 修复方案（Supabase，免费、无需配置域名）
- 预约改为「每条预约一个记录」存入云端表 `appointments`：工作台实时监听全部，客户端按**手机号**只看自己的 → 既实时，又**不泄露其他客户数据**。
- 客户/会员/交易等其它数据存入 `sync` 表，用于工作台之间（手机/iPad/电脑）互通与备份。
- 客户扫码链接自动带上 Supabase 配置（`?sb_url=...&sb_key=...#customer`），**客户无需任何配置**即可把预约实时推上云。

> 为什么从腾讯云开发换成 Supabase？云开发免费但**必须手动加「安全来源/授权域名」白名单**（`13477092494.github.io`），不配就会被浏览器 CORS 拦截。Supabase 免费且**无需配域名、无需开匿名登录开关**，对新手更省事，所以切换了。

## 你必须做的「一次性」设置（约 3 分钟，免费）
1. 注册并登录 [Supabase](https://supabase.com)（免费），新建一个项目。
2. 左侧 **SQL Editor → New query**，粘贴下面建表语句并执行（创建 `appointments`/`sync` 两张表，已开放匿名读写并开启实时订阅）：
   ```sql
   create table if not exists public.appointments(
     id text primary key,
     phone text default '',
     data jsonb
   );
   create table if not exists public.sync(
     id text primary key,
     data jsonb
   );
   alter table public.appointments enable row level security;
   create policy "anon all appt" on public.appointments for all to anon using (true) with check (true);
   alter table public.sync enable row level security;
   create policy "anon all sync" on public.sync for all to anon using (true) with check (true);
   alter publication supabase_realtime add table public.appointments;
   alter publication supabase_realtime add table public.sync;
   ```
3. **Project Settings → API**，复制 **Project URL** 与 **anon public key**（这串 key 是公开安全的，可放心填，不是私密密钥）。
4. 打开工作台 → **设置 → 云端同步**，粘贴这两项，点「连接并开启」。
   - 开启后，客户二维码会自动带上配置；把二维码给客户即可。
   - 其它工作台设备（如 iPad）首次也粘贴同一配置开启一次，之后自动连接。

## 已验证（用模拟 Supabase SDK 自动化测试）
- ✅ 客户提交预约 → 工作台**实时收到**并自动建立客户档案、弹出「🔔 新预约」提醒 + 提示音。
- ✅ 客户端**只看得到自己的预约**，其它客户数据不进本地（隐私隔离）。
- ✅ 工作台新建/取消/改状态的预约，实时同步回云端。
- ✅ 分享链接与二维码自动携带 Supabase 配置。
- ✅ 改动已部署到 GitHub Pages，三个链接均 200：
  - 工作台：https://13477092494.github.io/hemanli/
  - 客户端：https://13477092494.github.io/hemanli/#customer
  - 演示：https://13477092494.github.io/hemanli/demo.html

> 未开启云端时，App 行为与之前完全一致（数据仅在本机），无任何影响。

## 如何确认是否连上（在你自己的手机/iPad/电脑上）
1. 打开工作台：https://13477092494.github.io/hemanli/
2. 进入 **设置 → 云端同步**，看顶部「状态」：
   - 显示 **✅ 已连接** → 成功！让客户扫二维码预约，工作台会弹「🔔 新预约」。
   - 显示 **❌ 连接失败** → 把失败提示发我，常见原因：
     - `relation "public.appointments" does not exist`：第 2 步建表语句没执行，去 SQL Editor 执行一下。
     - `permission denied / JWT`：建表语句里的 RLS 策略没执行；重新执行整段 SQL。
     - `Failed to fetch / 网络错误`：Project URL 填错，或网络访问不了 Supabase（换个网络/确认项目未暂停）。
3. 真机验证最快：连上后，用手机扫工作台里的二维码下一单，工作台应立即收到提醒。
